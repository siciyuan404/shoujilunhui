# 2026-08-25 工作日志

## 手机回收报价平台 - 自定义域名穿透
- 用户要求用 `sj.6200052.xyz` 作为手机报价查询平台的固定外网域名（替代会变化的 quick tunnel 随机地址）。
- 方案：新建独立命名隧道 `sj`（ID `afa0cbf9-7325-44b6-aee9-3d2530fdd2b6`），不动用户原有的 `code-6200052` 隧道（服务 code/dev/comfy 子域名），避免闪断。
- 执行步骤：
  1. `cloudflared tunnel create sj` 建隧道
  2. `cloudflared tunnel route dns <UUID> sj.6200052.xyz` 加 DNS（注意：用 UUID 而非名称，否则会错误指向默认 config.yml 里的 code-6200052 隧道导致超时）
  3. 写 `C:/Users/62744/.cloudflared/sj.yml`：ingress 把 `sj.6200052.xyz` → `http://127.0.0.1:8760`
  4. `cloudflared tunnel --config sj.yml run` 后台启动
- 现状：本地 8760 服务器 + sj 隧道(PID 16152) 在跑，外网 `https://sj.6200052.xyz/` 返回 200，标题/数据正常。旧的随机 quick 隧道已杀。
- ⚠️ 已知：本会话早些排查 404 时把 `code-6200052` 隧道杀掉后未重启，用户 code/dev/comfy 子域名目前可能不在外网。如需恢复，用 `cloudflared tunnel --config C:/Users/62744/.cloudflared/config.yml run` 重启。
- 日常更新流程不变：用户每天发报价单图片 → 解析更新 `F:/git/shoujilulunhui/phone-price/data.json` → 刷新页面即最新价（内外网同步）。

## 验机档案 - 扩充小米/红米机型
- 用户要求"先把小米的补充了" → 在 `phone-specs.json` 新增 13 款小米/红米主流机型（原 5 款→18 款；全库 53→66 款）。
- 新增清单：小米15 Pro/14 Pro/14 Ultra/13/13 Pro/13 Ultra/Civi 4 Pro；红米K70 Pro/K70 至尊版/K60/K60 至尊版/Note 14 Pro/Note 13 Pro。
- 芯片（已联网核对 ZOL/mi.com/gsmarena）：小米15 Pro=骁龙8 Elite；14 Pro/14 Ultra=骁龙8 Gen3；13全系=骁龙8 Gen2；Civi 4 Pro=骁龙8s Gen3；红米K70 Pro=骁龙8 Gen3；K70 至尊版=天玑9300+；K60=骁龙8+ Gen1；K60 至尊版=天玑9200+；Note 14 Pro=天玑7300 Ultra；Note 13 Pro=骁龙7s Gen2。
- 每款字段：chip/releaseYear/features/bombLines/note/motherboardDesc/sources 齐全（bombLines 含小米账号锁、FRP锁等基线8项+机型特有项）。
- 外观图：复用 Commons API 脚本补图，18 款中小米/红米 13 款有图；5 款 Commons 无图（小米14 Pro、小米13 Pro、红米K70 Pro、红米K60 至尊版、红米Note 13 Pro）→ 详情页显示"暂无网络外观图片"占位。
- 踩坑：Commons 误匹配——"小米13" 一度命中 "Xiaomi 13T"（海外版，不同机）；已回滚并修掉小米13 Pro 串用小米13 图的问题。结论：Commons 对国行小米/红米新机覆盖弱，需严格排除 13T/Pro+/Ultra 变体。
- 发现预存 bug（非小米范围）：三星 Galaxy S24 与 S24 Ultra 共用同一张外观图（不同机型，错误）。待用户确认是否修。
- 验证：公网 `https://sj.6200052.xyz/phone-specs.json` 已同步 66 款（含新增），详情页点击新机型可进档案，缺图有占位兜底。

## Web UI 优化 - 详情页重设计
- 用户要求"web ui优化一下，重新设计详情页"。重写了 `index.html` 的详情视图（保留查价/验机列表模式不变）。
- 新设计：品牌渐变 Hero（机型名+芯片徽章+发布年份）→ 桌面双栏（`.detail-body` grid 320px|1fr，左栏 `position:sticky`）→ 左栏=外观卡片(大图+缩略图画廊，点缩略图 `swapDetailImg` 切换主图)+速览卡(报价/电流参考)；右栏=规格速览卡、🧩主板结构卡、⚠️炸弹机红线卡(红边框)、💡验机备注卡(黄)、🔗来源卡，统一 `.info-card` 圆角卡片 + 左侧色条标题(.bar)。
- 替换了原 DETAIL VIEW CSS 块（154-194）与 `renderDetail` 函数；新增 `swapDetailImg` 辅助函数。未改动验机列表样式（其 `.chip-badge` 绿标为避免全局污染改用独立的 `.hero-chip`）。
- 验证：本地 :8760 与公网 `https://sj.6200052.xyz/` 均 200，新类名(detail-body/hero-chip/appearance-card/quick-stats/swapDetailImg)均命中；`node --check` 内联 JS 通过。

## Web UI 优化 - 图片点击放大(lightbox)
- 用户要求"点击图片可以放大查看细节才好"。给详情页外观图/缩略图/主板图加了全屏看图层。
- 实现：DOM `#lightbox`（固定遮罩+关闭按钮+提示）；`openLightbox(src)` 打开并置 body overflow hidden；点图片 `toggleZoom` 切原始分辨率(`.zoom` + `.can-scroll` 可滚动看细节)；`closeLightbox` 关闭；`Esc` 键关闭。外观图/主板图 css 加 `cursor:zoom-in` 提示可点。
- 验证：`node --check` 通过；公网已含 openLightbox/closeLightbox/toggleZoom/id="lightbox" 标记。
